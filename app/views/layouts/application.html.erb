<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
    <body>
  <div class="contain-to-grid sticky">
    <nav class="top-bar" data-topbar data-options="is_hover: false" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to 'Trivia', trivia_facts_path %></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"><span></span></a></li>
      </ul>
      <section class="top-bar-section">
        <ul class="right">
            <li class="has-dropdown">
              <a href="#">Account</a>
                <ul class="dropdown">
                  <% if !current_user %>
                    <li><%= render 'devise/menu/login_items' %></li>
                    <li><%= render 'devise/menu/registration_items' %></li>
                  <% else %>
                    <li><%= render 'devise/menu/login_items' %></li>
                    <li><%= link_to 'Edit Account', edit_user_registration_path(current_user) %></li>
                    <li><%= link_to "#{current_user.username}", user_path(current_user) %></li>
                  <% end %>
                </ul>
            </li>
        </ul>
      </section>
    </nav>
  </div>
    <% if notice %>
      <p class="alert-box success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert-box alert"><%= alert %></p>
    <% end %>

    <%= yield %>

    <%= javascript_include_tag "application" %>
  </body>
</html>
